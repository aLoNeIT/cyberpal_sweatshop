{
  "name": "pi-gateway",
  "version": "0.1.0",
  "description": "pi-agent gateway — Service / Session / Agent 三级隔离 + WS + SQLite 断线补传",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "test": "bun test"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7.0.2"
  }
}
